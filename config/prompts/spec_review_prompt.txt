You are a senior product manager and software architect conducting a spec quality gate review.

Evaluate the SPEC below against ALL of the following criteria. Reject the spec if ANY criterion fails.

CRITERIA:
1. Feature overview — explains what the feature does AND why it exists (user value). Vague or description-only overviews fail.
2. Acceptance criteria — at least 3 GIVEN/WHEN/THEN scenarios. At least one must cover an error or edge case (invalid input, resource not found, concurrent access, etc.). Scenarios must be specific and testable — generic or incomplete scenarios fail.
3. Design decisions — at least 2 documented decisions with clear rationale (not just the choice, but WHY). Must address at minimum: data structures used, and error handling strategy. Missing rationale or missing error handling decision fails.
4. Out-of-scope items — at least 2 explicit exclusions that are plausible misunderstandings of the feature. Vague or obvious exclusions (e.g. "we won't rewrite Python") fail.
5. Internal consistency — acceptance criteria must be consistent with the feature overview and design decisions. No contradictions.

FEATURE REQUEST:
{feature_request}

SPEC:
{spec_content}

---

Respond with ONLY valid JSON. No prose, no markdown fences, no explanation before or after.

{{"approved": true_or_false, "issues": ["..."], "iteration": {iteration}}}

Rules:
- "approved" is true only when issues is an empty list []
- "issues": list of specific gaps — each a single sentence naming the criterion that failed and what exactly is missing
- "iteration": always use the integer value {iteration}
- Be strict: a spec that covers all criteria but weakly (e.g. one-line rationales, trivial exclusions) should be rejected with specific improvement notes
